<?xml version="1.0"?>
<configuration>
  <property>
    <name>javax.jdo.option.ConnectionURL</name>
    <value>jdbc:postgresql://postgres:5432/hive_metastore</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionDriverName</name>
    <value>org.postgresql.Driver</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionUserName</name>
    <value>hive</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionPassword</name>
    <value>hive</value>
  </property>
  <property>
    <name>hive.metastore.warehouse.dir</name>
    <value>/user/hive/warehouse</value>
  </property>
  <property>
    <name>hive.metastore.uris</name>
    <value>thrift://hive-metastore:9083</value>
  </property>
  <property>
    <name>hive.server2.thrift.bind.host</name>
    <value>0.0.0.0</value>
  </property>
  <property>
    <name>hive.server2.thrift.port</name>
    <value>10000</value>
  </property>
  <property>
    <name>metastore.event.db.notification.api.auth</name>
    <value>false</value>
  </property>
  <property>
    <name>hive.execution.engine</name>
    <value>tez</value>
  </property>
  <property>
    <!-- Real Hive 4.2.1 bug: the first GROUP BY query using COUNT (any
         form: COUNT(*), COUNT(1), COUNT(col)) compiled in a fresh
         HiveServer2 session throws a compile time
         ArrayIndexOutOfBoundsException, but only if it's the first
         GROUP BY query in that session; a GROUP BY using AVG/SUM first,
         or the exact same COUNT query as a later statement, works fine.
         Isolated by disabling pieces one at a time (vectorization, table
         stats); only turning off the CBO fixed it, pointing at a
         Calcite/CBO planning bug rather than storage or vectorization.
         Costs some query plan optimization; worth it for correctness on
         a dataset this small. -->
    <name>hive.cbo.enable</name>
    <value>false</value>
  </property>
</configuration>
